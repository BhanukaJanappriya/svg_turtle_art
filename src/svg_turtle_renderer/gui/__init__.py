"""The desktop dashboard.

A tkinter control panel that queues SVG files and drives the renderer on an
embedded turtle canvas, so the whole thing lives in one window that stays open
until the user closes it.
"""
